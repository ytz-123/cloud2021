server:
  port: 8001

spring:
  applacation:
    name: cloud-payment-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource       #当前数据源的操作类型
#    driver-class-name: com.mysql.jdbc.Driver         #MySQL驱动包
    driver-class-name: org.gjt.mm.mysql.Driver         #MySQL驱动包
    url: jdbc:mysql://localhost:3306/pay?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: root
    druid:
      validation-query: select 1
      test-while-idle: true

mybatis:
  mapperLocations: classpath:mapper/*.xml
  type-aliases-package: com.ytz.springcloud.entities    #所有Entity别名类所在包
